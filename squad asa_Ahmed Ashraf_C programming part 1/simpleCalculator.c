#include <stdio.h>
int main() {
    int firstNumber;
    int secondNumber;
    do{
        scanf(%f, &firstNumber);
        scanf(%f, &secondNumber);
        if(!isdigit(firstNumber) || !isdigit(secondNumber)){
            printf("Please enter digit values")
        }
    }
}